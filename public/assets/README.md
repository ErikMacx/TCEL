# Assets

Drop binary assets here. They are served from the site root, e.g.
`public/assets/headshot.jpg` is available at `/assets/headshot.jpg`.

To confirm and add:

- **headshot.jpg** — Eric's photo. Replaces the "EM" monogram placeholder in the
  About section of the homepage (`src/pages/index.astro`, the `.portrait` block).
- **og.jpg** — 1200x630 social share image. Wire into `Base.astro` as `og:image`.
